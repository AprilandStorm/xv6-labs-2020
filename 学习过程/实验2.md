- 开始实验前，需要切换到本次实验的分支：syscall
# 知识点
主要是用来熟悉系统调用的流程，了解系统调用的步骤，然后为XV6系统增加新的系统调用函数
- 在操作系统中，系统调用是用户程序与内核交互的关键机制。用户程序无法直接操作硬件，也不能直接访问内核提供的资源。系统调用充当桥梁，允许用户程序通过受控的接口向内核请求服务，这样可以保持系统的安全性和稳定性。
- 添加新的系统调用涉及到的文件如下（仅包括添加系统调用，不包括系统调用的实现）
```
user/user.h  //用户态程序调用跳板函数
user/usys.pl //跳板函数使用CPU提供的ecall指令，切换到内核态
kernel/syscall.c //内核态下调用系统调用处理函数syscall(), 所有系统调用都会跳到这里来处理
kernel/syscall.h //设定系统调用号，syscall()根据跳板传进来的系统调用编号，查询syscalls[]表，找到对应的内核函数并台调用
```
# 实验work1
## System call tracing
- 实现trace功能，跟踪用户程序中调用的系统调用，并将调用的相关信息打印出来
- user/trace.c代码
```
#include "kernel/param.h"
#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int
main(int argc, char *argv[])
{
  int i;
  char *nargv[MAXARG];//nargv[] 是一个新的参数数组，用来保存将要执行的命令和参数（从 argv[2] 开始复制）;MAXARG 是一个宏，应该在其他地方定义，比如定义为 32 或 64，表示最大参数个数。
   //argc < 3: 参数太少;(argv[1][0] < '0' || argv[1][0] > '9')：第一个参数不是数字开头，可能不是合法的 mask;如果不满足条件：输出错误信息
  if(argc < 3 || (argv[1][0] < '0' || argv[1][0] > '9')){
    fprintf(2, "Usage: %s mask command\n", argv[0]);
    exit(1);
  }
  //argv[1] 是字符串，比如 "5"，调用 atoi 转为整数 5;trace(5)：这是一个自定义系统调用，功能是设置系统调用追踪的掩码（mask）;类似 xv6 的 trace() 系统调用，掩码用于控制要追踪哪些系统调用;如果失败，输出错误，退出
  if (trace(atoi(argv[1])) < 0) {
    fprintf(2, "%s: trace failed\n", argv[0]);
    exit(1);
  }
  //把命令和参数（从 argv[2] 开始）复制到 nargv[0], nargv[1], …中
  for(i = 2; i < argc && i < MAXARG; i++){
    nargv[i-2] = argv[i];
  }
  //exec()：系统调用，用于用另一个程序替换当前进程;nargv[0] 是程序名，如 "ls";nargv 是参数数组，以 NULL 结尾（此处应依赖实现自动结束）;如果 exec() 成功，当前进程就会被 "ls" 进程替代，不会再往下执行;如果 exec() 失败，继续执行 exit(0);，虽然一般不会执行到
  exec(nargv[0], nargv);
  exit(0);
}
```
- 例子1
```
$ trace 32 grep hello README
3 : syscall read -> 1023
3 : syscall read -> 966
3 : syscall read -> 70
3 : syscall read -> 0
```
trace 32表示启用了跟踪功能，32表示跟踪系统调用号掩码为5的系统调用（1<<5 =32）；在kernel/syscall.h中，`#define SYS_read  5`,所以要跟踪read系统调用；然后执行`grep hello README`命令，即用grep搜索README文件中的hello\
打印输出行，3表示运行grep的进程ID，syscall read表示发生了read系统调用，-> 1023表示调用read的返回值，这里表示读取了1023字节\
- 例子2
```
$ trace 2147483647 grep hello README
```
2147483647的低31位为1，表示跟踪所有系统调用

### 修改代码（实现上述功能）
#### 用户态修改
- 首先在用户空间的头文件`user/user.h`加入函数声明，让用户态程序可以找到这个跳板入口函数
- 在`user/usys.pl`中，加入用户态到内核态的跳板函数\
该文件实一个Perl脚本，用于生成usys.S文件，包含用户态程序调用内核态系统调用的接口
- 由于user/trace.c实现的是一个用户空间的函数，所以需要在Makefile中添加`U/_trace\`
#### 修改内核态
- 首先在`kernel/syscall.h`中添加trace的系统调用号（掩码）
- 在`kernel/syscall.c`中全局声明trace系统调用处理函数，并且把系统调用号与处理函数关联
- 在`kernel/proc.h`中，在进程类proc结构中，添加kama_syscall_trace属性，用掩码的方式记录要跟踪的系统调用
- 上一步中添加了进程的属性，所以在一些相关的地方也要做些修改\
首先是新进程的初始化中队这个属性设置默认值0，在`kernel/proc.c`中\
修改fork函数（在`kernel/proc.c`中），使得子进程能继承父进程的这个属性\
- 在`kernel/sysproc.c`中实现这个系统调用，这里实现的是，为进程的kama_syscall_trace属性赋值用户程序传来的要跟踪的系统调用号
- 因为是跟踪系统调用，所以可以在触发系统调用后就按题设要求打印数据。所有的系统调用都会在`kernel/syscall.c`中的`syscall()`函数中进行处理\
此处将进程的kama_syscall_trace属性值右移num位，然后和1做与运算，以此来判断当前进程是否跟踪系统调用号为num的系统调用
- 上一步打出日志的过程还需要知道系统调用的名称字符串，这里定义一个字符串数组进行映射

#实验work2
该实现需要添加一个系统调用`sysinfo`，用来获取空闲的内存、已创建的进程数量
- 在`kernel/sysinfo.h`中已经声明好了`freemem`空闲内存属性和`nproc`进程数量属性，我们需要把获取的数据赋值在其中；在`user/user.h`、`user/usys.pl`、`Makefile`中添加代码
- 在内核态`kernel/syscall.h`添加sysinfo系统调用号
- 在'kernel/syscall.c`中添加系统调用映射
- 实现sysinfo函数\
在`user/sysinfotest.c`中可以看到，用户程序调用sysinfo系统调用的时候，传入的参数为sysinfo结构体。在获取用户空间的sysnifo结构体地址后，要把内核空间的sysinfo结构体的数据复制过去，这样用户程序才能拿到数据
- 实现获取空闲空间你内存的kama-freebytes函数，在内存相关`kernel/kalloc.c`中实现；xv6采用了一种简单的空闲链表机制来记录空闲的物理内存页，空闲页自身作为链表节点，指向下一个空闲页，只要遍历这个链表就能计算空闲内存
- 实现统计进程数量的函数kama_procnum，该函数在进程相关`kernel/proc.c`中实现；在这个文件中有一个进程表`struct proc proc.c`记录了所有的进程，而每一个进程都有一个state属性，表示该进程是否使用；因此可以遍历proc进程表，判断当前进程是否在使用，是的话数量+1
- 因为上述的两个函数不像系统调用那样使用了extern全局声明，`kernel/sysproc.c`中的sys_sysinfo是找不到这两个函数的。这里要在内核头文件`kernel/defs.h`声明这两个函数
- 实验2完成
