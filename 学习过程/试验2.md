- 开始实验前，需要切换到本次实验的分支：syscall
# 知识点
主要是用来熟悉系统调用的流程，了解系统调用的步骤，然后为XV6系统增加新的系统调用函数
- 添加新的系统调用涉及到的文件如下（仅包括添加系统调用，不包括系统调用的实现）
```
user/user.h  //用户态程序调用跳板函数
user/usys.pl //跳板函数使用CPU提供的ecall指令，切换到内核态
kernel/syscall.c //内核态下调用系统调用处理函数syscall(), 所有系统调用都会跳到这里来处理
kernel/syscall.h //设定系统调用号，syscall()根据跳板传进来的系统调用编号，查询syscalls[]表，找到对应的内核函数并台调用
```
# 实验
## System call tracing
- 实现trace功能，跟踪用户程序中调用的系统调用，并将调用的相关信息打印出来
