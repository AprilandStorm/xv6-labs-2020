# DMA
- DMA，全称是 Direct Memory Access（直接内存访问），指的是一种不经过 CPU，由外设直接与系统内存之间进行数据传输的技术。
- 通常情况下，外设（比如网卡、硬盘、显卡等）需要读取或写入数据时，都是通过 CPU 作为中介来完成的。这意味着 CPU 必须不断地处理这些数据传输请求，这会消耗大量的 CPU 资源，特别是在数据量很大的时候，会显著降低系统的整体性能。
- 
